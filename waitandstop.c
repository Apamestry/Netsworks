#include <stdio.h>
int main()
{
    int n,t= 0;
    int tt=2;
    int q;
    int frame[100];

    printf("Enter the number of frames to send: ");
    scanf("%d", &n);

    for (int i = 0; i < n; i++)
    {
        printf("Enter the %d frame data:", i);
        scanf("%s", &frame[i]);
    }



    while (1)
    {
        t++;

    }
    
}
